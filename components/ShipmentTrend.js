import { Card, CardContent } from "@material-ui/core";

const ShipmentTrend = () => {
  return (
    <Card variant="outlined" className="shipment">
      <CardContent className="border-b p-4">
        <h2 className="h2">Shipment Trend</h2>
      </CardContent>
      <CardContent>
        <div></div>
      </CardContent>
    </Card>
  );
};

export default ShipmentTrend;
